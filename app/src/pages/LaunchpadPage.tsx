import { LaunchpadWorkspace } from '../components/LaunchpadWorkspace'

/** Standalone Launchpad surface — second-screen V1 experience. */
export function LaunchpadPage() {
  return <LaunchpadWorkspace />
}
